<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shinemo.score.dal.score.mapper.UserTmpMapper">

	<!-- 结果集对应关系 -->
	<resultMap id="result" type="com.shinemo.score.client.score.domain.UserTmp" >
		<id column="id" property="id"/>
		<result property="mobile" column="mobile"/>
		<result property="videoId" column="video_id"/>
		<result property="score" column="score"/>
	</resultMap>

	<!-- 数据库字段 -->
	<sql id="columns">
		id, mobile, video_id, score
	</sql>

    <sql id="table">
        user_tmp
    </sql>

    <!-- 查询条件 -->
    <sql id="conditions">
        <where>
            <choose>
                <when test="id != null">and id = #{id}</when>
                <otherwise>
					<if test="mobile != null">and mobile = #{mobile}</if>
					<if test="videoId != null">and video_id = #{videoId}</if>
                </otherwise>
            </choose>
        </where>
    </sql>


	<!-- 查询结果集 -->
    <select id="find" resultMap="result"
            parameterType="com.shinemo.score.client.score.query.UserTmpQuery">
        select
        <include refid="columns"/>
        from
        <include refid="table"/>
        <include refid="conditions"/>
        <if test="orderByEnable and orderByList != null and orderByList.size() > 0">
            order by
            <foreach collection="orderByList" item="orderBy" separator=",">
                ${orderBy.column} ${orderBy.sort}
            </foreach>
        </if>
        <if test="pageEnable">
            limit #{startRow}, #{pageSize}
        </if>
    </select>

	<!-- 查询某条记录 -->
    <select id="get" resultMap="result" parameterType="com.shinemo.score.client.score.query.UserTmpQuery">
        select
        <include refid="columns"/>
        from
        <include refid="table"/>
        <include refid="conditions"/>
        limit 1
    </select>

	<!-- 统计查询结果数量 -->
    <select id="count" resultType="long" parameterType="com.shinemo.score.client.score.query.UserTmpQuery">
        select count(*) from
        <include refid="table"/>
        <include refid="conditions"/>
    </select>

	<!-- 删除 -->
    <delete id="delete" parameterType="com.shinemo.score.client.score.query.UserTmpQuery">
        delete from
        <include refid="table"/>
        where id = #{id}
    </delete>

	<!-- 插入 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id"
            parameterType="com.shinemo.score.client.score.domain.UserTmp">
        insert into
        <include refid="table"/>
        <trim prefix="(" suffix=")" suffixOverrides=",">
                    <if test="id != null">
						id,
					</if>
                    <if test="mobile != null">
						mobile,
					</if>
                    <if test="videoId != null">
						video_id,
					</if>
                    <if test="score != null">
						score,
					</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
                    <if test="id != null">
						#{id, jdbcType=BIGINT},
                    </if>
                    <if test="mobile != null">
						#{mobile, jdbcType=VARCHAR},
                    </if>
                    <if test="videoId != null">
						#{videoId, jdbcType=VARCHAR},
                    </if>
                    <if test="score != null">
						#{score, jdbcType=INTEGER},
                    </if>
        </trim>
    </insert>

    <!-- 更新 -->
    <update id="update" parameterType="com.shinemo.score.client.score.domain.UserTmp">
        UPDATE user_tmp
        <set>
            <if test="mobile != null">
                mobile = #{mobile, jdbcType=VARCHAR},
            </if>
            <if test="videoId != null">
                video_id = #{videoId, jdbcType=VARCHAR},
            </if>
            <if test="score != null">
                score = #{score, jdbcType=INTEGER}
            </if>
        </set>
        WHERE
        id = #{id}
    </update>
</mapper>